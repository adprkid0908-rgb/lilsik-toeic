# 기본적으로 모든 파일을 제외하고, 배포에 필요한 것만 아래에 명시적으로 허용한다.
# 이 폴더에는 학습 자료 원본과 개인 토큰 파일이 함께 있어 실수로 커밋되는 것을 막기 위한 구성이다.
# 새로 추가한 파일을 GitHub에 올리려면 여기에 예외 규칙(!파일명)을 먼저 추가해야 한다.
*

!.gitignore
!README.md
!index.html
